[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "geullint-training"
version = "0.1.0"
description = "Reproducible local ranking-data tools for GeulLint"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
onnx = ["onnx>=1.16"]
torch = ["torch>=2.3"]
